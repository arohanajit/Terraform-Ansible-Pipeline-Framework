---
# Application Server Role Default Variables

# Application Configuration
appserver_app_dir: "/var/www/app"
appserver_app_user: "app_user"
appserver_app_group: "app_group"

# Environment Configuration
appserver_app_env: "production"
appserver_app_port: 3000
appserver_app_node_env: "production"

# Process Management
appserver_pm2_instances: 4
